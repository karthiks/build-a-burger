import React from 'react';
import { shallow } from 'enzyme';
import Layout from './layout';

const COMPONENT_UNDER_TEST = '<Layout/>';
describe(COMPONENT_UNDER_TEST, ()=>{
    it.skip('should have a Toolbar component', ()=>{});
});